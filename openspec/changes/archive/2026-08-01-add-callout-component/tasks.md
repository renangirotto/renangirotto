## 1. Component

- [x] 1.1 Create `src/components/Callout.astro` with `title`/`text` props and markup for header (icon + title) and body (text), divided by a border line
- [x] 1.2 Style the header: 16px padding all sides, 18px (1.125rem) title font-size, flex layout with 24x24px `CircleQuestionMark` icon (used `CircleHelp` — see note)
- [x] 1.3 Style the body: 16px padding all sides except 24px `padding-block-end`, 16px (1rem) text font-size
- [x] 1.4 Apply `light-dark()` theming for background, divider, and text/icon color using the specified tokens

## 2. Verification

- [x] 2.1 Render `Callout` on a page in both light and dark mode and visually compare against the provided screenshots
